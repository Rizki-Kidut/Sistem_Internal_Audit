import { useState, type FormEvent } from 'react';
import { FileCheck, LogIn } from 'lucide-react';
import { useAuth } from '../../contexts/AuthContext';
import { Button } from '../ui';
import { Input } from '../ui/Field';

export function LoginPage(){const {login}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState<string|null>(null);async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError(null);try{await login(email,password);}catch(e){setError(e instanceof Error?e.message:'Login gagal');setBusy(false);}}return <main className="min-h-screen bg-gradient-to-br from-blue-900 via-blue-800 to-slate-900 flex items-center justify-center p-6"><div className="w-full max-w-md bg-white rounded-2xl shadow-2xl p-8"><div className="flex items-center gap-3 mb-8"><div className="w-12 h-12 rounded-xl bg-blue-800 text-white flex items-center justify-center"><FileCheck/></div><div><h1 className="text-2xl font-bold text-gray-900">CertiTrack</h1><p className="text-sm text-gray-500">Internal Audit</p></div></div><h2 className="text-lg font-semibold">Masuk ke aplikasi</h2><p className="text-sm text-gray-500 mt-1 mb-6">Gunakan akun identitas yang telah disiapkan administrator.</p>{error&&<div className="mb-4 p-3 rounded-lg border border-red-200 bg-red-50 text-sm text-red-700">{error}</div>}<form onSubmit={submit} className="space-y-4"><label className="block text-sm font-medium">Email<Input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-sm font-medium">Password<Input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><Button type="submit" className="w-full justify-center" disabled={busy}>{busy?'Memeriksa akun...':<><LogIn size={16}/> Login</>}</Button></form></div></main>}
